# 2026-07-24 工作日志

## 策略可行性分析（AT0 日内做T）
- 用户问"当前策略是否可行"。核查后关键发现：
  - `outputs/backtest/batch_summary.json`（13:50，20股/654笔/净+26,430/100%配对胜率/20-20全盈利）跑在 **synthetic 合成行情**上，非真实市场。缓存分布 21 synthetic / 54 baostock；候选池用的 2026-06-22~2026-07-22 窗口全是 synthetic。
  - `gen_synthetic_bars.py:11` 明确"围绕 VWAP 做均值回归"生成 → 任何均值回归策略在其上必漂亮，不能证明实盘可行。
  - 真实数据诊断 `data_diagnosis_51.json`：revert_rate 0.21-0.32、continue_rate 0.58-0.68（真实中多数偏离是延续不是回归）→ 合成数据高估了真实表现。
  - 真实 baostock 缓存存在（54 个），如 600176_2025-07-24_2026-07-22.json（241日）。
- 结论：工程/风控可行✅；实盘盈利⚠️证据不足（仅合成验证）。动作=等待证据（真实行情回测）。
- 环境坑：系统 `python` segfault（解释器损坏）；受管 python 缺 at0 依赖；node 路径要用 `D:/...` 绝对路径（别用 `/d/...`，会被当相对路径）。
- 产出：`outputs/research_feasibility_current_strategy.md`（可行性分析）、`outputs/research_memo_expired_leg_tier1.md`（Tier1整改备忘录）。

## 买卖策略代码总结（strategy.py / execution.py / thresholds.yaml）
- 用户要"根据代码总结当前买卖策略和具体指标"。已读 strategy.py(execution.py), thresholds.yaml。
- 当前策略 = 混合：买腿(reduce→实为sell开仓)走**趋势跟随**；卖腿(add→buy)侧仍有P0-5**均值回归**结构。但文件头 docstring(P0-5, lines 30-38)仍描述"双边均值回归"，与 2026-07-24 改写后的 reduce 趋势跟随实现**不一致**——属文档/代码背离，已向用户指出。
- 关键参数：open_max_vwap_dev=1.2%、动态止损 floor 1.5% + |open_vwap_dev|×0.8、max_holding_bars 频率自适应(默认12)、max_t_size_ratio 25%、round_trip_cost 0.3%、min_capture_spread 0.75%。
- 具体指标：VWAP_dev/ATR/BB(20,2σ)/EMA20/RSI14(30/70)/KDJ(9,3,3;20/80)/MFI(20,80)/ADX(25/40)/量比(5/20;缩<0.8扩>1.0)/OR/主动买卖比(>55%)/regime(HOT/COOL/COLD)。

## 落地 intrabar 真实止损（方案C2，根因 #2）
- 背景（盈亏结构分解）：5股×近一年，止损腿 159 条 -49278 是唯一亏损源；止损腿均亏 -310 ≫ 0.5%×fill×3000≈-150，因用 bar.close 作止损价时价格已远超阈值。0.5% 止损对均值回归策略致命。
- 改动（execution.py + backtest.py）：
  - TradeLeg 增 `stop_fill_price` 字段（触发价位，非 bar.close）
  - update_holding 用 bar_low/bar_high 算 max_adverse/max_favorable（盘中穿透即计入）
  - check_stop_loss 穿透止损线时按触发价 fill_price±threshold 成交
  - backtest.py 传入 bar.get(low/high)，止损盈亏/成交价/风险事件统一用 stop_fill_price
- 验证（上轮已记录）：600000 净 +390.32（改前 +231.63）；Phase C 回归 14/14；20股优化基线 +29610.70（改前 +14598.94）。根因修复成立，止损亏损精确绑定阈值，消除 bar.close 约2×放大。
- 关键：信号层 evaluate_all_signals(strategy.py) 与执行层 execution.py 独立，方案C2 只动执行层，不影响信号测试。

## verify_l5 现状与根因（非本轮回测改动）
- 当前 93/96（重跑），通过数在 92~94 间浮动 → `evaluate_all_signals` 进程内全局状态污染（重复调用返回不同结果），测试需单独治理。
- 3 处失败：冲高缩量触发 reduce(got none) / reduce_score≥3(got 2) / 下探企稳触发 add(got none)。
- 根因1（add）：`open_max_vwap_dev=1.2%` 上限（加仓分支设计正确，防追深跌途中）与测试需深回踩触发超卖不兼容。用户选"调 dip 深度至1.2%内"经证伪是死路：浅回踩(≤1.2%)→KDJ/RSI到不了超卖→score=2→仍none；深回踩(>1.2%)→被上限否决→none。无深度可同时满足。
- 根因2（reduce）：`evaluate_reduce_signal` 极值层仅 vwap_dev<0（价低于VWAP）触发，但测试 bars_spike 是冲高缩量(vwap_dev=+3.11%)，方向相反——测试语义写错，非信号错，早于本轮回测。
- 用户定：**标记已知漂移，保持现状**（不修测试、不改生产）。verify_l5.py 已加注释标记；92~94 浮动为已知现象。下一步转真实行情回测验证（可行性分析真实瓶颈）。

## 八层 Alpha Engine 架构方案评审
- 用户提出把 AT0 升级为八层引擎（Universe/Regime/Alpha/AdaptiveThreshold/Position/Execution/Pair/OnlineLearning），要求评审。
- 评审产出：`docs/alpha_engine_proposal_review.md`。核心结论=方向认同、节奏反对，三条红线：
  1. Online Learning 自动更新权重/阈值违反 §9.2（未经人工审核不改风控阈值）→ 降级为离线周报建议+人工审批。
  2. 样本量（20股×23日≈78笔配对）撑不起六 regime 分层与权重学习 → P0-7 数据扩容为一切前置。
  3. Best-Match 配对是统计幻觉高危区（底仓做T下真实 PnL=全现金流之和，与配对方式无关，Best-Match 只美化报表）→ 保留 FIFO。
- 修订路线：Phase 0 = Tier 1 双跑验证 + 数据扩容（硬前置）；Phase 1 = 测量层先行（IC/RankIC/Alpha Decay/MAE-MFE/分层回测/参数平台）；Phase 2 = 连续评分改造（与现有 layer_scores 计分结构衔接）；Phase 3 = 单点动态化（仅 VWAP 阈值）+ 仓位；Phase 4 = 数据就绪后再评估。
- 用户同时要求"写某公司首次覆盖报告"但未指明公司，已请其明确标的。
